# Ingestion Utilities
- Shared pipeline helpers live in this folder (chunking, diffing, cache management).
- Files to implement next:
  - `utils.ts`: batching + retry helpers (created).
  - `cache.ts`: module singleton for movies/actors/genres (created).
  - Upcoming: orchestrator + staging helpers.

